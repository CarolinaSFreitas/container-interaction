name: "My first container action"
description: "Lab Cloud Guru"
author: "Carol"
inputs:
  myInput:
    description: "greeting to use"
    required: true
    default: "Awesome Guru"
  outputs:
    myOutput:
      description: "total memory"
  runs:
    using: 'docker'
    image: 'Dockerfile'
